services:
  - type: web
    name: mixmaster-app
    env: node
    region: oregon
    plan: free
    buildCommand: npm run build:full
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
    autoDeploy: true
